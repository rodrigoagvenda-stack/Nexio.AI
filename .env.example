# ============================================================
# IPVB SISTEMA — Variáveis de Ambiente
# Configure todas estas variáveis no EasyPanel (Environment)
# NUNCA comite valores reais neste arquivo
# ============================================================

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://wzohmrckjnrpqzizszsi.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=            # Supabase → Settings → API → anon public
SUPABASE_SERVICE_ROLE_KEY=                # Supabase → Settings → API → service_role (SECRET)

# --- App ---
NEXT_PUBLIC_APP_URL=https://ipvb.seudominio.com
NODE_ENV=production

# --- Storage ---
STORAGE_BUCKET=ipvb-arquivos
